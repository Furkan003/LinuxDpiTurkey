/target
**/*.rs.bk
Cargo.lock.bak

# Uretilen apt deposu -- gh-pages dalinda yayinlaniyor, burada durmaz.
depo/

# Tauri / frontend (ileride)
node_modules/
dist/

# Makineye özel derleme yolu (OneDrive dışı)
.cargo/config.toml
